import Link from "next/link"; import { forgotPassword } from "../login/actions"; import { Button } from "@/components/ui/button"; import { Input } from "@/components/ui/input"; import { Label } from "@/components/ui/label";
export default async function ForgotPage({searchParams}:{searchParams:Promise<{message?:string;error?:string}>}){const p=await searchParams;return <main className="grid min-h-[calc(100vh-4rem)] place-items-center px-5 py-10"><form action={forgotPassword} className="w-full max-w-sm rounded-lg border bg-card p-6 shadow-xl"><h1 className="text-2xl font-semibold">Quên mật khẩu</h1><p className="mb-6 mt-2 text-sm text-muted-foreground">Nhập email để nhận liên kết đặt lại mật khẩu.</p>{p.message&&<p className="mb-4 rounded-md bg-primary/10 p-3 text-sm text-primary">{p.message}</p>}{p.error&&<p className="mb-4 rounded-md bg-destructive/10 p-3 text-sm text-destructive">{p.error}</p>}<div className="space-y-2"><Label htmlFor="email">Email</Label><Input id="email" name="email" type="email" required autoComplete="email"/></div><Button className="mt-6 w-full">Gửi liên kết</Button><p className="mt-5 text-center text-sm"><Link href="/login" className="text-primary hover:underline">Quay lại đăng nhập</Link></p></form></main>}