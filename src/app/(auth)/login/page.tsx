import Link from "next/link";
import { signIn } from "./actions";
import { AuthSubmitButton } from "@/components/auth-submit-button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
export default async function LoginPage({searchParams}:{searchParams:Promise<{message?:string;error?:string}>}){const p=await searchParams;return <main className="grid min-h-[calc(100vh-4rem)] place-items-center px-5 py-10"><form aria-busy="false" action={signIn} className="w-full max-w-sm rounded-lg border bg-card p-6 shadow-xl"><h1 className="text-2xl font-semibold">Đăng nhập</h1><p className="mb-6 mt-2 text-sm text-muted-foreground">Tiếp tục tới UXLens dashboard.</p>{p.message&&<p className="mb-4 rounded-md bg-primary/10 p-3 text-sm text-primary">{p.message}</p>}{p.error&&<p className="mb-4 rounded-md bg-destructive/10 p-3 text-sm text-destructive">{p.error}</p>}<div className="space-y-2"><Label htmlFor="email">Email</Label><Input id="email" name="email" type="email" required autoComplete="email" placeholder="you@company.com"/></div><div className="mt-4 space-y-2"><div className="flex justify-between"><Label htmlFor="password">Mật khẩu</Label><Link href="/forgot-password" className="text-xs text-primary hover:underline">Quên mật khẩu?</Link></div><Input id="password" name="password" type="password" required minLength={8} autoComplete="current-password"/></div><AuthSubmitButton>Đăng nhập</AuthSubmitButton><p className="mt-5 text-center text-sm text-muted-foreground">Chưa có tài khoản? <Link href="/register" className="font-medium text-primary hover:underline">Đăng ký</Link></p></form></main>}