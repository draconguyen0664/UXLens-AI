import Link from "next/link"; import { signUp } from "../login/actions"; import { Button } from "@/components/ui/button"; import { Input } from "@/components/ui/input"; import { Label } from "@/components/ui/label";
export default async function RegisterPage({searchParams}:{searchParams:Promise<{error?:string}>}){const p=await searchParams;return <main className="grid min-h-[calc(100vh-4rem)] place-items-center px-5 py-10"><form action={signUp} className="w-full max-w-sm rounded-lg border bg-card p-6 shadow-xl"><h1 className="text-2xl font-semibold">Tạo tài khoản</h1><p className="mb-6 mt-2 text-sm text-muted-foreground">Bắt đầu audit UX miễn phí.</p>{p.error&&<p className="mb-4 rounded-md bg-destructive/10 p-3 text-sm text-destructive">{p.error}</p>}<div className="space-y-2"><Label htmlFor="fullName">Họ và tên</Label><Input id="fullName" name="fullName" required autoComplete="name"/></div><div className="mt-4 space-y-2"><Label htmlFor="email">Email</Label><Input id="email" name="email" type="email" required autoComplete="email"/></div><div className="mt-4 space-y-2"><Label htmlFor="password">Mật khẩu</Label><Input id="password" name="password" type="password" minLength={8} required autoComplete="new-password"/></div><div className="mt-4 space-y-2"><Label htmlFor="confirm">Xác nhận mật khẩu</Label><Input id="confirm" name="confirm" type="password" minLength={8} required autoComplete="new-password"/></div><Button className="mt-6 w-full">Đăng ký</Button><p className="mt-5 text-center text-sm text-muted-foreground">Đã có tài khoản? <Link href="/login" className="font-medium text-primary hover:underline">Đăng nhập</Link></p></form></main>}