import { NextResponse } from "next/server";
import { createClient } from "@/lib/supabase/server";
import { getAppUrl } from "@/lib/stripe";
export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){const{id}=await params;const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)return NextResponse.json({error:"Unauthorized"},{status:401});const body=await request.json() as {isPublic?:boolean};const{data,error}=await supabase.from("audits").update({is_public:Boolean(body.isPublic)}).eq("id",id).eq("user_id",user.id).eq("status","completed").select("share_token,is_public").single();if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({...data,url:`${getAppUrl()}/share/${data.share_token}`})}
