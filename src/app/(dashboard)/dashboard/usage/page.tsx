import { BarChart3 } from "lucide-react";
import { Card,CardContent,CardHeader,CardTitle } from "@/components/ui/card";
import { createClient } from "@/lib/supabase/server";
import { isSupabaseConfigured } from "@/lib/supabase/config";
const LIMIT=3;
export default async function UsagePage(){let count=2;let pro=false;if(isSupabaseConfigured()){const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(user){const period=new Date().toISOString().slice(0,7);const[{data:usage},{data:subscription}]=await Promise.all([supabase.from("usage").select("audit_count").eq("period",period).maybeSingle(),supabase.from("subscriptions").select("status,plan").maybeSingle()]);count=usage?.audit_count??0;pro=Boolean(subscription&&["active","trialing"].includes(subscription.status)&&subscription.plan!=="free")}}const percent=pro?100:Math.min(100,count/LIMIT*100);return <div className="mx-auto max-w-6xl"><h1 className="text-3xl font-bold">Sử dụng</h1><p className="mb-8 mt-2 text-muted-foreground">Theo dõi quota audit của workspace.</p><Card><CardHeader><CardTitle>Tháng hiện tại</CardTitle></CardHeader><CardContent><BarChart3 className="size-6 text-primary"/><div className="mt-5 flex items-end justify-between"><span className="text-4xl font-bold">{count}</span><span className="text-sm text-muted-foreground">{pro?`${count} audits · Pro không giới hạn`:`${count} / ${LIMIT} Free audits`}</span></div><div className="mt-4 h-2 rounded-full bg-muted"><div className="h-full rounded-full bg-primary transition-all" style={{width:`${percent}%`}}/></div><p className="mt-3 text-sm text-muted-foreground">{pro?"Gói Pro đang hoạt động.":count>=LIMIT?"Bạn đã hết lượt tháng này. Nâng cấp để tiếp tục.":`Còn ${LIMIT-count} lượt miễn phí. Lượt chỉ bị trừ khi audit hoàn thành.`}</p></CardContent></Card></div>}
