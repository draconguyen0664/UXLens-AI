import Link from "next/link";
import { ArrowRight, BarChart3, Clock3, FileSearch, Plus, Sparkles, TrendingUp } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
const stats = [{ label: "Tổng audits", value: "12", change: "+3 tháng này", icon: FileSearch }, { label: "Điểm trung bình", value: "78", change: "+6 điểm", icon: TrendingUp }, { label: "Issues đã tìm", value: "47", change: "9 critical", icon: BarChart3 }];
const recent = [{ name: "Mobile checkout", score: 78, issues: 8, time: "12 phút trước" }, { name: "SaaS onboarding", score: 84, issues: 5, time: "Hôm qua" }, { name: "Pricing page", score: 71, issues: 11, time: "3 ngày trước" }];
export const metadata = { title: "Dashboard" };
export default function DashboardPage() { return <div className="mx-auto max-w-6xl"><header className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-sm font-medium text-primary">Design intelligence</p><h1 className="mt-1 text-3xl font-bold tracking-tight">Chào mừng trở lại</h1><p className="mt-2 text-muted-foreground">Theo dõi chất lượng UX và bắt đầu audit mới.</p></div><Button asChild><Link href="/dashboard/new"><Plus className="mr-2 size-4"/>New Audit</Link></Button></header><section className="mt-8 grid gap-4 sm:grid-cols-3">{stats.map(({label,value,change,icon:Icon})=><Card data-stat-card key={label} className="relative overflow-hidden"><CardContent className="p-5"><div className="flex items-center justify-between"><span className="text-sm text-muted-foreground">{label}</span><Icon className="size-4 text-primary"/></div><p className="mt-3 text-3xl font-bold">{value}</p><p className="mt-1 text-xs text-muted-foreground">{change}</p></CardContent></Card>)}</section><section className="mt-6 grid gap-6 lg:grid-cols-[1fr_300px]"><Card><CardHeader className="flex-row items-center justify-between"><CardTitle>Audits gần đây</CardTitle><Button asChild variant="ghost" size="sm"><Link href="/dashboard/audits">Xem tất cả <ArrowRight className="ml-1 size-4"/></Link></Button></CardHeader><CardContent><div className="space-y-2">{recent.map((audit,i)=><Link href={i===0?"/dashboard/audits/demo":"#"} key={audit.name} className="flex items-center gap-4 rounded-lg border p-3 transition hover:border-primary/40 hover:bg-accent/50"><span className="grid size-10 shrink-0 place-items-center rounded-md bg-primary/10"><FileSearch className="size-4 text-primary"/></span><div className="min-w-0 flex-1"><p className="truncate text-sm font-medium">{audit.name}</p><p className="mt-0.5 flex items-center gap-1 text-xs text-muted-foreground"><Clock3 className="size-3"/>{audit.time}</p></div><div className="text-right"><p className="font-semibold text-primary">{audit.score}/100</p><p className="text-xs text-muted-foreground">{audit.issues} issues</p></div></Link>)}</div></CardContent></Card><div className="space-y-6"><Card><CardHeader><CardTitle className="text-base">Usage tháng này</CardTitle></CardHeader><CardContent><div className="flex items-end justify-between"><span className="text-3xl font-bold">3</span><span className="text-sm text-muted-foreground">/ 3 audits</span></div><div className="mt-3 h-2 overflow-hidden rounded-full bg-muted"><div className="h-full w-full bg-primary"/></div><p className="mt-3 text-xs text-muted-foreground">Đã dùng hết lượt Free. Nâng cấp để tiếp tục.</p></CardContent></Card><Card className="border-primary/30 bg-primary/5"><CardContent className="p-5"><Sparkles className="size-5 text-primary"/><p className="mt-3 font-semibold">Audit không giới hạn</p><p className="mt-1 text-sm text-muted-foreground">Nâng cấp Pro chỉ $9/tháng.</p><Button asChild className="mt-4 w-full" size="sm"><Link href="/pricing">Nâng cấp Pro</Link></Button></CardContent></Card></div></section></div>; }